# Label

The `Label` component is used to create a label for an input field.

```tsx
import { TextField } from 'react-aria-components';
import { Label } from '@/ui/components/Label';
import { Input } from '@/ui/components/Input';

export default function Example() {
  return (
    <Label>First name</Label>
  );
}
```

## TextField

```tsx
import { TextField } from 'react-aria-components';
import { Label } from '@/ui/components/Label';
import { Input } from '@/ui/components/Input';

export default function Example() {
  return (
    <TextField className="grid gap-1.5" isInvalid>
      <Label>First name</Label>
      <Input />
    </TextField>
  );
}
```

## Checkbox

```tsx
import { Checkbox } from '@/ui/components/Checkbox';

export default function Example() {
  return (
    <Checkbox
      isSelected={true}
      onChange={function(){}}
    >
      Checkbox
    </Checkbox>
  );
}
```
