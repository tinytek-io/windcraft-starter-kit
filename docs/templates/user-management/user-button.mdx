# UserButton

The `UserButton` component is a button that displays the user's name and avatar. When clicked, it opens a dropdown menu with options to sign out or navigate to the user's profile.

import {Card} from '@/ui/components/Card';
import {UserButton} from '@/ui/templates/UserButton/UserButton';

<Card className='w-full'>
  <UserButton />
</Card>