# Verify Code Form

The `VerifyCodeForm` template is used to verify a user's email or phone number by entering a code sent to them.

import {Card} from '@/ui/components/Card';
import {VerifyCodeForm} from '@/ui/templates/VerifyCodeForm';

<Card className='w-full'>
  <VerifyCodeForm />
</Card>