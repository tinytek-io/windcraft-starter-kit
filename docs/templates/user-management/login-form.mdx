# Login Form

The `LoginForm` template is used to verify a user's email or phone number by entering a code sent to them.

import { Card } from '@/ui/components/Card';
import { LoginForm } from '@/ui/templates/LoginForm';

<Card className='w-full'>
  <LoginForm />
</Card>